package com.example.linkedin.model;

/**
 * Enum representing the status of a response in the workflow.
 */
public enum ResponseStatus {
    GENERATED,
    APPROVED,
    POSTED,
    FAILED,
    REJECTED
}
